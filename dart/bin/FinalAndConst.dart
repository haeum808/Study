// 상수 - 항상 같은 수 (값)
void main() {
  // final - 최초에 값이 한번 할당되면 다시 할당할 수 없습니다.
  final int testVal;
  testVal = 30;
  // const - 최초에 값이 한번 할당되면 다시 할당할 수 없다. + 선언과 동시에 값을 할당해야 된다 (해당 값은 컴파일 시점에서 결정되어야만 한다)
  const int testVal2 = 10;
}
